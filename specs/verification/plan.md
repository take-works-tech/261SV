---
status: draft
updated: 2026-09-23
---

# Verification plan

How each acceptance criterion is checked: by what test, with what data, to what tolerance.

Automated rows are written as `(planned)` until a test exists: the intent is recorded now, and the
row starts demanding a real, existing test the moment the word `planned` is removed. That removal is
the moment the criterion begins claiming coverage - and from 2026-09-18 the linter checks the
**named test**, not merely the file it sits in, so a row cannot claim a test nobody wrote.

**Which of the two a row is, is counted separately.** `check_specs.py --report` prints how many
criteria have a row here and how many have a test, per feature. The two were one number until
2026-09-18 and it read as the larger of them: 384 of 384 criteria "with a verification", while most
rows were intentions. The honest figure is the second one.

**Fixture data is named here and its origin recorded.** A tolerance measured against a sample of
unknown origin proves nothing, and months later nobody can tell whether it was representative.

Fixtures are generated by the test code itself wherever the toolkit can write the format - EnSight,
Exodus, IOSS, VTKHDF, VTK XML including partitioned files, STL - so they carry no third-party terms
(XC-085). CGNS and the Offered-tier formats cannot be written, so those rows are marked manual and
name a locally held file rather than a committed one. **A generated fixture only contains the problems
we thought of**, which is why the manual rows exist rather than being quietly dropped.

## Workspace, cases and variables

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| workspace/AC-001 | integration (planned) | - | `fixtures/ws_three_levels` | authored by hand for this test | exact | child appears at the requested depth and reopens there |
| workspace/AC-002 | integration (planned) | - | `fixtures/ws_three_levels` | as above | exact | confirmation names the exact descendant count |
| workspace/AC-003 | unit (planned) | - | in-test hierarchy | authored in the test | exact | operation refused, hierarchy byte-identical afterwards |
| workspace/AC-004 | integration (planned) | - | `fixtures/ws_inherit` | authored, two inheriting children | exact | every inheriting descendant changes in one operation |
| workspace/AC-005 | integration (planned) | - | `fixtures/ws_inherit` | as above | exact | overriding child unchanged and shown as overridden |
| workspace/AC-006 | integration (planned) | - | `fixtures/ws_inherit` | as above | exact | child-only variable absent from the parent |
| workspace/AC-007 | unit (planned) | - | in-test hierarchy | authored in the test | exact | refusal names the ancestor that defines it |
| workspace/AC-008 | unit (planned) | - | in-test hierarchy | authored in the test | exact | reported unresolved, no value substituted |
| workspace/AC-009 | integration (planned) | - | `fixtures/ws_inherit` | as above | exact | later variable change reaches the bound input |
| workspace/AC-010 | unit (planned) | - | in-test variables | authored in the test | exact | binding refused, both units named |
| workspace/AC-011 | integration | tests/test_handlers.py::test_a_declaration_saved_is_the_declaration_the_next_session_finds | `fixtures/ws_full` + `fixtures/case_small` | authored workspace over the small dataset | exact | exported artefacts identical before and after reopen |
| workspace/AC-012 | integration (planned) | - | `fixtures/ws_full` with one input removed | as above, file deleted by the test | exact | case marked unresolved, workspace file unmodified on disk |
| workspace/AC-013 | integration (planned) | - | `fixtures/ws_corrupt` | valid workspace truncated at a byte boundary | exact | original file byte-identical after the failed open |
| workspace/AC-014 | integration (planned) | - | `fixtures/ws_tags` | authored, 5 tagged cases | exact | hidden count matches the filtered-out set |
| workspace/AC-015 | integration (planned) | - | `fixtures/ws_tags` | as above | exact | selection stays visible and marked outside the filter |
| workspace/AC-016 | manual | - | `fixtures/import_series` | a real parameter sweep, origin recorded with the fixture | - | proposal is reasonable to an engineer; judged, not asserted |
| workspace/AC-017 | integration (planned) | - | `fixtures/import_series` | as above | exact | same grouping is not proposed again in the session |

## Result ingest

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| ingest/AC-020 | integration | tests/test_reader.py::test_reads_a_grid_and_lists_its_fields, src/ui/logic/drop.test.ts and src/ui/state/engine.connected.test.ts against a real engine | `fixtures/case_small` | a small public reference dataset, source recorded with the fixture | exact | every field listed with its association |
| ingest/AC-021 | integration | tests/test_regression_catalogue.py, tests/test_reader.py::test_unsupported_format_names_itself, src/ui/logic/drop.test.ts and src/ui/state/engine.connected.test.ts against a real engine | `fixtures/unsupported.bin` | random bytes with a foreign extension | exact | format named, no case created |
| ingest/AC-049 | integration | src/ui/logic/drop.test.ts and src/ui/state/engine.connected.test.ts against a real engine | the sample's two recorded files dropped together in the reverse order; one recorded and one unrecorded file; one recorded file alone with the other case on screen; a file recorded twice, two files for one case, a name match at another path | generated by the engine (XC-298) and authored in the tests | exact | both loaded in the document's order and the View area keeps its case; nothing loaded and the unrecorded file named; the file goes to the case that records it; each failing file named with why; a name match goes to the shown case, not the recorded one (XC-301) |
| ingest/AC-050 | unit plus integration | tests/test_missing_values.py::TestTheLoadAnswer and src/ui/state/engine.connected.test.ts against a real engine | the holed grid loaded | generated by tests/demo_case.py | exact | missingCount 3, 1 and 2 for its three fields (XC-303) |
| ingest/AC-051 | integration | tests/test_regression_catalogue.py and tests/test_incomplete_files.py::TestABinarySTLIsHeldToItsHeader | the 46 fixtures of tests/regression_catalogue.py, each written and read; random bytes, a cut and a padded binary STL | generated by the tests (XC-085) | exact | every reader has a whole and a broken fixture; each fixture reads as its kind says; the STL cases are refused naming the sizes; the list of Verified formats without a reader is held empty (XC-307, XC-308) |
| ingest/AC-052 | integration | tests/test_ensight_vtkhdf.py, tests/test_regression_catalogue.py and tests/test_non_ascii_paths.py::TestEveryReaderReadsAtSuchAPath | EnSight Gold by the toolkit's writer (one part; two parts over two steps), ASCII and both binary byte orders by hand, VTKHDF by the toolkit's writer; each cut in the case, the geometry and a variable file; random bytes and an empty file; the forms the check does not follow | generated by the tests (XC-085) | exact | whole files read with the values written; every cut, missing or unreadable file is refused naming the case and the file within seconds; a dropped variable is a result lost; the unfollowed forms are refused by name; both read at a path outside ASCII (XC-308) |
| ingest/AC-022 | integration | tests/test_regression_catalogue.py, tests/test_reader.py::test_truncated_file_is_reported, tests/test_incomplete_files.py::test_a_file_cut_short_is_refused_or_reads_as_the_complete_file, tests/test_handlers.py::TestAFileThatIsNotAllThere and tests/test_paths.py::TestAFileAnotherProcessHolds | every format this build reads, cut at 50, 80, 90, 95, 99 and 99.9 per cent and one byte short; a partitioned set with a piece cut | written and cut by the test | exact | each cut is refused or reads with every value identical to the complete file; an Exodus cut the reader would zero-fill is refused naming both lengths (E-212); the load is a refusal and the workspace unchanged |
| ingest/AC-023 | integration | tests/test_reader.py::test_no_unit_is_declared_on_load | `fixtures/case_small` | as above | exact | every field shows no declared unit on load |
| ingest/AC-024 | unit | tests/test_units.py::test_undeclared_unit_refuses_conversion | in-test dataset | authored in the test | exact | conversion refused with an undeclared-unit reason |
| ingest/AC-025 | unit | tests/test_units.py::test_a_field_carries_its_declaration_only_when_given_one | `fixtures/case_small` | as above | exact | export carries the undeclared marker |
| ingest/AC-026 | integration (planned) | - | `fixtures/case_series_partitioned` | multi-step partitioned set, origin recorded | exact | step and part counts match the fixture manifest |
| ingest/AC-027 | unit plus integration | tests/test_reader.py::TestACompositeIsOneCaseOfManyParts and tests/test_cgns.py::TestAZoneTheReaderCannotReadIsAnAbsence, and src/ui/state/engine.connected.test.ts against a real engine | a composite with a named empty block; a CGNS file whose second zone has a name and no coordinates; the same file loaded through the store | generated by the test | exact | the case is partial with the absence named; the maximum carries the partial caveat; the store keeps the mark with the missing part's name and the load's warning says so |
| ingest/AC-028 | unit | tests/test_reader.py::test_source_frame_is_recorded | in-test geometry | authored in the test | 1e-12 relative | converted coordinates match the expected transform |
| ingest/AC-029 | unit (planned) | - | in-test geometry with an unsupported frame | authored in the test | exact | import refused, unsupported element named |
| ingest/AC-030 | integration (planned) | - | `fixtures/case_large` | generated to exceed the display budget, generator script recorded | exact | view marked reduced |
| ingest/AC-031 | end-to-end | tests/test_prototype_thread.py::test_a_display_budget_of_one_triangle_does_not_move_the_reported_maximum | a hexahedron reduced to one triangle | generated by the test | exact | reported maximum equals the value computed on the full dataset |

| ingest/AC-032 | integration | tests/test_reader.py::test_support_level_is_reported_per_format | one file of each support level | fixtures, origins recorded | exact | the level shown matches the declared table in XC-049 |
| ingest/AC-033 | integration | tests/test_reader.py::test_partitioned_reader_declares_its_gap | an OpenFOAM case | public tutorial case, origin recorded | exact | the named gaps match that reader's documented limits |
| ingest/AC-034 | integration (planned) | - | a CGNS file carrying DimensionalUnits | authored with the CGNS tools | exact | the unit stays undeclared, and the file's own unit is not shown as if read |
| ingest/AC-047 | integration, and the frozen engine | tests/test_non_ascii_paths.py, src/ui/state/engine.connected.test.ts and src/shell/engine-process.test.ts under a directory named outside ASCII, and the thread packaging/freeze_engine.py walks on the frozen engine | every reader, the document, the export, the engine's directories, at `解析 結果／ｒｕｎ－１ #1 (a&b) 📐` | written by the test | exact | read and written as at any path; the Exodus family refused by name before its library sees the path outside the UTF-8 code page, and read in it (E-216) |
| ingest/AC-048 | integration | tests/test_long_paths.py | every reader, the document, the export and the engine's directories at a directory past 300 characters, and a long source beside a short document | written by the test through the extended form | exact | read, recorded, reopened and written as at a short path; records and answers plain; the plain long path still fails where the policy is off (E-217) |
| ingest/AC-046 | unit plus integration | tests/test_incomplete_files.py::TestAFileMustHoldStill, tests/test_handlers.py::TestAFileThatIsNotAllThere and tests/test_paths.py::TestAFileThatVanishes | a file appended to during the read by the reader itself; a file appended to after the load, then asked for another step | generated by the test | exact | refused naming the file, the sizes and the times; the step in memory still answers, and no step is read from the changed file |
| ingest/AC-045 | unit plus integration | src/ui/logic/information.test.ts and src/ui/state/engine.connected.test.ts against a real engine | the store after a load, hand-built and from a real engine | generated in the test | exact | the view holds the engine's counts, bounds, parts, fields and axis; nothing from a fixture; the not-answered list names the reader version, cell types, checksum, frame and import time |

workspace/AC-016 is manual because "a reasonable proposal" is a judgement, not a threshold. Recording that
honestly is better than an automated check that passes while the proposal is useless.

## Report generation

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| report/AC-001 | end-to-end (planned) | - | `fixtures/case_small` | as ingest | exact | file opens offline in a current browser, model rotates |
| report/AC-036 | integration | tests/test_exported_document_opens.py | the exported document and a probe with an inline script, an inline module, inline style and a data-URI image, opened from a local file, from the file marked ZoneId=3 and through the administrative share, in Edge and Chrome | authored in the test | exact | every marker present and the document's DOM identical the three ways; skips naming the reason where no browser or share is present (E-218) |
| report/AC-037 | integration | tests/test_exported_document_prints.py | five documents - short, one and two page breaks, a figure at page width, a long text then a figure and a table, a whole report - printed to PDF by Edge and Chrome | authored in the test, with the stand-in font | exact | the sheets read back from the PDF: A4, one sheet per page break, the figure whole with its heading, the same count in both browsers; skips naming the reason where no browser is present (E-219) |
| report/AC-045 | integration | tests/test_missing_values.py::TestAGraphAndADeliverable | a value table over the holed grid's point and cell fields | generated by tests/demo_case.py | exact | each number says 欠測 N 件を除いて計算した値です; no value cell is blank, nan or a dash (XC-303) |
| report/AC-046 | integration | tests/test_exported_document_opens.py::TestTheDocumentClaimsWhatWasMeasured and TestTheProbeRunsEverywhereADeliverableGoes::test_each_feature_the_document_relies_on | the exported document's footer; VERIFIED_BROWSERS against the browsers present; the probe of every CSS feature the stylesheet relies on and a data-URI font face | authored in the test | exact | each named browser appears with its version and the unverified sentence; a browser measured is claimed no newer than measured and an unclaimed one is a warning; every feature supported, the page rule parsed, the face tried; skips naming the reason where no browser is present (E-228) |
| report/AC-002 | end-to-end | tests/test_prototype_thread.py::test_the_probe_the_statistics_and_the_document_agree_with_what_the_file_stores | a float32 field whose maximum has no exact representation | generated by the test | exact | every displayed value present as text in the document |
| report/AC-003 | integration (planned) | - | `fixtures/case_large` | as ingest | exact | reduction marked, numbers equal full-data values |
| report/AC-004 | integration (planned) | - | `fixtures/case_series_partitioned` minus one part | as ingest | exact | coverage statement present in the document |
| report/AC-005 | integration (planned) | - | `fixtures/case_small` | as above | exact | PowerPoint values equal HTML values field by field |
| report/AC-006 | integration (planned) | - | `fixtures/case_small` | as above | exact | substitution stated where a format cannot carry an element |
| report/AC-007 | integration | tests/test_handlers.py::TestExportingADeliverable and src/ui/state/engine.connected.test.ts against a real engine | the demo workspace with one loaded case and a declared unit | generated in the test | exact | `report.provenance` names the workspace, the case, the source file with its modification time, the declared unit and the product version; the exported document names the source; the area shows the same facts and, where the engine refuses them, the refusal as the item that blocks the export |
| report/AC-008 | integration (planned) | - | `fixtures/case_small` | as above | exact | undeclared field carries the marker, no guessed unit |
| report/AC-009 | manual | - | two art styles authored for the test | authored here | - | exported document visually follows the selected style |
| report/AC-010 | integration (planned) | - | art style with a removed asset | authored here | exact | default used and the missing asset named |
| report/AC-011 | integration (planned) | - | `fixtures/case_small` + recorded model responses | responses captured and stored with the fixture | exact | generated passages marked and their values named |
| report/AC-012 | integration (planned) | - | recorded response asserting an absent value | authored here | exact | the statement is omitted from the document |
| report/AC-014 | integration | tests/test_report_html.py::test_writing_is_refused_until_the_list_is_accepted | a document holding a view this build cannot carry | authored here | exact | every dropped element is named before the file is written |
| report/AC-015 | end-to-end (planned) | - | a case annotated in Japanese and Simplified Chinese | authored here | exact | the document renders the text on a machine with only Latin fonts installed |
| report/AC-016 | integration (planned) | - | a string with a character outside the embedded subset | authored here | exact | the character and its element are named; no empty box is written |
| report/AC-013 | end-to-end (planned) | - | `fixtures/case_small`, no model configured | as above | exact | report produced offline with mechanical summary only |

## Assistant and headless operation

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| assistant/AC-001 | integration (planned) | - | recorded instruction set | authored here | exact | executed through the same command surface as the interface |
| assistant/AC-002 | integration (planned) | - | a three-command instruction | authored here | exact | one undo restores the prior state exactly |
| assistant/AC-003 | integration (planned) | - | an unmappable instruction | authored here | exact | nothing changed, reason reported |
| assistant/AC-004 | integration (planned) | - | instruction whose second command fails | authored here | exact | full rollback, failing part named |
| assistant/AC-005 | integration (planned) | - | delete and inherited-variable instructions | authored here | exact | effect and affected count described before applying |
| assistant/AC-006 | integration (planned) | - | headless session without authorisation | authored here | exact | refused rather than assumed |
| assistant/AC-007 | integration (planned) | - | `fixtures/case_small` + a reference document | authored here | exact | origin named for every stated value |
| assistant/AC-008 | integration (planned) | - | reference document contradicting the data | authored here | exact | dataset value reported, disagreement stated |
| assistant/AC-009 | integration (planned) | - | question about an absent value | authored here | exact | reported unavailable, not taken from the document |
| assistant/AC-010 | integration (planned) | - | command surface enumeration | authored here | exact | operation set equals the interface operation set |
| assistant/AC-011 | integration (planned) | - | a scripted session | authored here | exact | audit log holds inputs, outcome and origin per operation |
| assistant/AC-012 | unit (planned) | - | malformed commands | authored here | exact | rejected with a named problem, no state change |
| assistant/AC-013 | end-to-end (planned) | - | no model configured, network blocked | as above | exact | every non-model operation completes |
| assistant/AC-014 | manual | - | a configured model | authored here | - | the data that would leave is stated before it leaves |
| assistant/AC-015 | integration (planned) | - | unreachable model endpoint | authored here | exact | reported, other operations unaffected |
| assistant/AC-016 | integration (planned) | - | a proposal request | authored here | exact | configuration built from templates, no generated code executed |
| assistant/AC-017 | integration (planned) | - | a request outside the template set | authored here | exact | refusal rather than approximation |

## View

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| view/AC-001 | integration (planned) | - | a saved view | authored here | exact | the stored definition contains identifiers only, no values or images |
| view/AC-002 | end-to-end (planned) | - | `fixtures/case_small` + a saved view | as ingest | exact | picture and reported numbers identical after reopening |
| view/AC-003 | integration (planned) | - | two views on one case | authored here | exact | both are listed and the active one is identified |
| view/AC-004 | integration (planned) | - | a view referencing an absent field | authored here | exact | element omitted and listed as unresolved, no substitute field used |
| view/AC-005 | integration (planned) | - | one view through each renderer | authored here | exact | reported values identical across renderers (INV-002) |
| view/AC-006 | integration | tests/test_handlers.py::test_capabilities_say_which_formats_and_which_renderers_with_their_requirements | a renderer the machine cannot run | simulated by capability probe | exact | names the renderer, its requirement, and an alternative |
| view/AC-007 | integration | tests/test_render.py::test_a_reduced_surface_still_reports_the_full_range | a display budget of one triangle | forced in the test | exact | view marked reduced, numbers from full data |
| view/AC-008 | manual | - | a case with a simple solid | authored here | - | three-view and dimensioned styles are recognisable as such to an engineer |
| view/AC-009 | integration (planned) | - | a dimension on a declared-unit case | authored here | exact | label carries the declared unit, or the undeclared marker |
| view/AC-010 | integration (planned) | - | `fixtures/case_large` with a dimension | as above | exact | measured value equals the full-data value |
| view/AC-011 | integration (planned) | - | assets in all three scopes | authored here | exact | scope shown for each applied asset |
| view/AC-012 | integration (planned) | - | an imported asset with stated terms | authored here | exact | terms recorded; export refused where they are not satisfied |
| view/AC-013 | integration (planned) | - | a view with a deleted asset | authored here | exact | default used and the missing asset named |
| view/AC-014 | integration (planned) | - | a template and a second workspace | authored here | exact | an independent View is created across workspaces |
| view/AC-015 | integration (planned) | - | a template with partly matching fields | authored here | exact | resolved and unresolved listed before creation or drawing |
| view/AC-016 | integration (planned) | - | a template matching nothing | authored here | exact | application refused with a reason |
| view/AC-017 | integration (planned) | - | `fixtures/case_series_partitioned` | as ingest | exact | playback available and the displayed step stated |
| view/AC-018 | integration (planned) | - | a series with a field missing at one step | authored here | exact | drawn as missing, not carried forward |
| view/AC-019 | integration (planned) | - | a saved view | authored here | exact | the same definition yields the same image bytes |
| view/AC-020 | integration (planned) | - | a camera path and a time-varying case | authored here | exact | video follows the path and states the time mapping |
| view/AC-021 | integration (planned) | - | no encoder available | simulated | exact | names the missing encoder and still writes frames |
| view/AC-022 | integration (planned) | - | a view exported to USD | authored here | exact | metersPerUnit and upAxis present in the file |
| view/AC-023 | integration (planned) | - | a view with an unrepresentable element | authored here | exact | element named before writing |

| workspace/AC-018 | integration (planned) | - | quantities of each provenance | authored here | exact | every entry shows provenance and unit or the undeclared marker |
| workspace/AC-019 | integration (planned) | - | a computed quantity | authored here | exact | the expression is shown |
| workspace/AC-020 | integration (planned) | - | a case the expression fails on | authored here | exact | shown as unavailable, not omitted |
| workspace/AC-021 | unit | tests/test_precision.py::test_a_field_reports_the_digits_of_the_type_it_was_stored_as | float32 and float64 fields | generated in the test | exact | digits shown never exceed the stored precision (INV-014) |

## Graph

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| graph/AC-001 | integration | tests/test_graph_definition.py::TestASeriesSaysWhatItIs and tests/test_handlers.py::TestAGraphOverTheLoadedCase | series of each provenance | authored here | exact | each series records quantity, unit and provenance |
| graph/AC-002 | integration | tests/test_graph_definition.py::TestAnUndeclaredUnitIsMarkedRatherThanAssumed and tests/test_handlers.py::TestAGraphOverTheLoadedCase | a quantity with no declared unit | authored here | exact | axis carries the undeclared marker |
| graph/AC-003 | unit plus integration | tests/test_graph_definition.py::TestIncompatibleUnitsAreRefusedNamingBoth and tests/test_handlers.py::TestAGraphOverTheLoadedCase | two series with incompatible units | authored here | exact | refused, both units named |
| graph/AC-004 | integration | tests/test_graph_definition.py::TestTheDefinitionIsWhatIsSaved and src/ui/state/engine.connected.test.ts against a real engine | a saved graph | authored here | exact | definition stored, not the plotted values |
| graph/AC-005 | integration (planned) | - | a case plus a reference file | authored here | exact | every available quantity offered |
| graph/AC-006 | integration (planned) | - | a computed series | authored here | exact | expression shown; computation happens in the analysis module |
| graph/AC-007 | integration (planned) | - | an expression failing on one case | authored here | exact | that case drawn as no data with a reason |
| graph/AC-008 | integration | tests/test_handlers.py::TestAGraphOverTheLoadedCase | no selection given, and the asking area's context given | authored here | exact | the loaded cases are plotted, or the context's, and the answer says which; a definition naming its cases wins (XC-292) |
| graph/AC-009 | integration (planned) | - | a declarative selection | authored here | exact | exactly the resolved cases plotted and listed |
| graph/AC-010 | integration (planned) | - | a selection matching nothing | authored here | exact | the emptying condition is named |
| graph/AC-011 | integration (planned) | - | failing selection code | authored here | exact | nothing selected, failure reported (XC-089) |
| graph/AC-012 | integration (planned) | - | a repeated study | authored here | exact | per-repeat and combined both available, choice stated |
| graph/AC-013 | integration | tests/test_graph_series.py::TestAMissingQuantityStaysVisible and tests/test_handlers.py::TestAGraphOverTheLoadedCase | a set with one case lacking the quantity | authored here | exact | drawn as no data and kept in the legend |
| graph/AC-028 | unit plus integration | tests/test_missing_values.py::TestAGraphAndADeliverable and src/ui/state/engine.connected.test.ts against a real engine | a series over the holed case and the all-missing case | generated by tests/demo_case.py | exact | the point carries missing-values and the count; the all-missing case is a gap with its reason (XC-303) |
| graph/AC-014 | manual | - | `fixtures/case_small` | as ingest | - | proposals are reasonable to an engineer and name their signal |
| graph/AC-015 | integration (planned) | - | recorded model responses | captured with the fixture | exact | proposals marked as inferred |
| graph/AC-016 | integration (planned) | - | a rejected proposal | authored here | exact | not repeated in the session |
| graph/AC-017 | integration (planned) | - | styles in all three scopes | authored here | exact | scope shown, partial application allowed |
| graph/AC-018 | integration (planned) | - | a template and a second workspace | authored here | exact | creates an independent Graph across workspaces |
| graph/AC-019 | integration (planned) | - | a template with unresolved series | authored here | exact | listed and drawn as no data |
| graph/AC-020 | integration (planned) | - | a saved graph | authored here | exact | exported values match the screen |
| graph/AC-021 | integration (planned) | - | a time-varying case | authored here | exact | animation exported with the time mapping stated |

## Report, continued

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| report/AC-017 | integration (planned) | - | a report with commentary requested | authored here | exact | depth offered; what is sent and its cost stated before sending |
| report/AC-018 | manual | - | a direction in words | authored here | - | the direction changes what is discussed and no value in the document |
| report/AC-019 | integration (planned) | - | a budget set below the need | authored here | exact | stops and reports what was produced |
| report/AC-020 | integration (planned) | - | a cited document | authored here | exact | document named, statement marked as from reference material |
| report/AC-021 | integration (planned) | - | a document contradicting the data | authored here | exact | the data value is carried, the disagreement stated |
| report/AC-022 | integration | tests/test_handlers.py::TestPartVisibility::test_the_document_s_figure_shows_what_the_view_shows, tests/test_handlers.py::TestExportingADeliverable::test_a_document_s_figure_keeps_its_bar_whatever_the_screen_asked and src/ui/state/engine.connected.test.ts | a view the interface wrote, with a part hidden | generated in the test | exact | the document's figure is drawn from the definition the document holds, a hidden part left out, and the interface's own frame comes from the same definition |
| report/AC-023 | integration (planned) | - | a time-varying case | as ingest | exact | video produced from a camera path with the time mapping stated |

## Pipeline

| Acceptance ID | Method | Test | Fixture or sample data | Where the data came from | Tolerance | Pass condition |
|---|---|---|---|---|---|---|
| pipeline/AC-001 | integration (planned) | - | workspace artefacts, a template library and an empty pipeline | authored here | exact | source-labelled pinned step added, or refused with a reason; never left invalid |
| pipeline/AC-002 | integration (planned) | - | a pipeline with a completed run | authored here | exact | order stored; earlier artefacts unchanged, asserted by hash |
| pipeline/AC-003 | integration (planned) | - | a pipeline whose referenced item, template or revision was deleted | authored here | exact | step kept and exact missing reference marked; run refused |
| pipeline/AC-004 | integration (planned) | - | two case units, four and two cases | authored here | exact | the target set reports four, then six |
| pipeline/AC-005 | integration (planned) | - | as above, a view unit after each case unit | as above | exact | the first view acts on four cases, the second on all six |
| pipeline/AC-006 | integration (planned) | - | a case unit holding a selection, six cases match | authored here | exact | resolved at run start and listed before anything runs |
| pipeline/AC-007 | integration (planned) | - | a view unit above any case unit | authored here | exact | skipped, stated as empty, run continues |
| pipeline/AC-008 | integration (planned) | - | the six-case study of the end-to-end scenario | authored here | exact | dry-run list equals what the real run then does; nothing written |
| pipeline/AC-009 | integration (planned) | - | a pipeline with a release step | authored here | exact | one authorisation naming the step and the case count |
| pipeline/AC-010 | integration (planned) | - | as above, authorisation declined | as above | exact | non-destructive steps ran; destructive reported as not authorised |
| pipeline/AC-011 | integration (planned) | - | six cases, one truncated file | generated by the test | exact | five complete; the sixth names the failed step and skips its rest |
| pipeline/AC-012 | integration (planned) | - | as above | as above | exact | no report file exists for the failed case; the skip is recorded |
| pipeline/AC-013 | integration (planned) | - | as above, stop-on-first-failure | as above | exact | stops at the first failure and reports what was written |
| pipeline/AC-014 | integration (planned) | - | a long run cancelled mid-study | authored here | exact | stops at a step boundary; completed work kept; boundary named |
| pipeline/AC-015 | integration (planned) | - | any completed run | authored here | exact | pipeline, version, cases and per-step outcome all present |
| pipeline/AC-016 | integration (planned) | - | the same pipeline run twice | authored here | exact | artefacts identical apart from recorded timestamps |
| pipeline/AC-017 | integration (planned) | - | a run producing cases | authored here | exact | each produced case names its producing step |
| pipeline/AC-018 | integration (planned) | - | six cases loaded, then a clear unit | as ingest | exact | target set empty, data released, source files hash-identical before and after |
| pipeline/AC-019 | integration (planned) | - | a case exceeding the configured limit | generated by the test | exact | required and available stated; the process survives |
| pipeline/AC-020 | unit (planned) | - | an edit one level past the limit | authored in the test | exact | refused, limit named |
| pipeline/AC-021 | integration (planned) | - | one pipeline, both entry points | authored here | exact | artefacts identical between headless and interactive runs |
| pipeline/AC-022 | integration (planned) | - | the six-case study, headless | as pipeline/AC-011 | exact | machine-readable outcome; exit code non-zero |
| pipeline/AC-023 | integration (planned) | - | six cases selected together | authored here | exact | exactly one case unit is created, holding six |
| pipeline/AC-024 | integration (planned) | - | a loop over three values with a condition inside | authored here | exact | dry run states the iteration count and the condition value |
| pipeline/AC-025 | unit (planned) | - | loop containing condition containing view | authored in the test | exact | depth counted as three |
| pipeline/AC-026 | integration (planned) | - | each of the three loop sources | authored here | exact | iterations equal the resolved count; the source is stated |
| pipeline/AC-027 | integration (planned) | - | a loop whose contents name the index | authored here | exact | each iteration sees its own index |
| pipeline/AC-028 | unit (planned) | - | a loop resolving to LIM-008 plus one | authored in the test | exact | refused before the run, unit and count named |
| pipeline/AC-029 | integration (planned) | - | a variable unit and a workspace variable of the same name | authored here | exact | units below see the binding; the workspace variable is unchanged |
| pipeline/AC-030 | unit (planned) | - | expressions over declared quantities | authored in the test | exact | the result carries the unit the expression produced |
| pipeline/AC-031 | unit (planned) | - | a length added to a time | authored in the test | exact | refused, both units named |
| pipeline/AC-032 | unit (planned) | - | an expression naming an unbound variable | authored in the test | exact | refused at edit time with the reference named |
| pipeline/AC-033 | integration (planned) | - | a condition true for three of six cases | authored here | exact | contents run for exactly those three |
| pipeline/AC-034 | integration (planned) | - | as above | as above | exact | the skipped contents record the value the expression evaluated to |
| pipeline/AC-035 | integration (planned) | - | one pipeline built twice, by script and by hand | authored here | exact | the stored documents are equal |
| pipeline/AC-036 | integration (planned) | - | a workspace carrying a script, and a stored pipeline | authored here | exact | opening executes nothing; running the pipeline starts no interpreter |
| pipeline/AC-037 | integration (planned) | - | a script making several changes | authored here | exact | all changes appear in the log and undo as one step |
| pipeline/AC-038 | integration (planned) | - | a fresh workspace | authored here | exact | unattended execution is off; a script is refused without authorisation |
| pipeline/AC-039 | integration (planned) | - | an action performed through the command surface | authored here | exact | the script text for that action is produced and re-runs to the same state |
| workspace/AC-022 | unit (planned) | - | objects of several kinds | authored in the test | exact | identifiers unique and never reused; references stored by identifier |
| workspace/AC-023 | unit (planned) | - | two cases named the same | authored in the test | exact | refused, the holder named, no suffix invented |
| workspace/AC-024 | unit (planned) | - | a name that exists and one that does not | authored in the test | exact | one object or a failure; never a list |
| workspace/AC-025 | integration (planned) | - | a renamed case referenced by a view and a pipeline | authored here | exact | both references still resolve after the rename |
| report/AC-024 | integration (planned) | - | commentary containing a banned category from each row of E-071 | authored here | exact | each offending statement is caught |
| report/AC-025 | integration (planned) | - | a statement that fails twice | authored here | exact | one rewrite attempted, then omitted; the count is stated |
| report/AC-026 | integration (planned) | - | passages using each of the five terms without their precondition | authored here | exact | each refused; each published when the precondition holds |
| report/AC-027 | integration (planned) | - | a single-mesh case | as ingest | exact | the report states discretisation error is unquantified |
| report/AC-028 | integration (planned) | - | three retrieved documents offered | authored here | exact | every citation resolves to one of the three by identifier |
| report/AC-029 | integration (planned) | - | a statement with no supporting document | authored here | exact | omitted; no composed reference appears |
| report/AC-030 | integration (planned) | - | a retrieved page, then changed | authored here | exact | address, date and retrieved text are all stored |
| report/AC-031 | integration (planned) | - | a case with no measured data | as ingest | exact | a limitations section is present |
| report/AC-032 | unit (planned) | - | two cases differing by a known amount | authored in the test | exact | magnitude, direction and the reference are all stated |
| report/AC-033 | unit (planned) | - | a difference below the tolerance | authored in the test | exact | reported as not distinguishable, neither equal nor changed |
| report/AC-034 | manual | - | a fresh installation | the installer under test | - | all five template shapes present, none branded or solver-specific |
| report/AC-035 | manual | - | the sample asset inventory and its licences | recorded with each asset | - | every shipped asset has redistributable terms, or is a placeholder |
| assistant/AC-018 | unit | tests/test_egress.py::TestAFreshWorkspacePermitsNothing and tests/test_egress.py::TestOfflineMeansNothingIsAttempted | a workspace with search not permitted, and a gate offline | authored in the test | exact | no request is made; the unanswerable question is named |
| assistant/AC-019 | unit | tests/test_egress.py::TestTheQueryIsShownBeforeItIsSent | a search with per-search confirmation on | authored in the test | exact | the query is shown and nothing is sent until confirmed |
| assistant/AC-020 | unit | tests/test_egress.py::TestWorkspaceContentIsWithheldFromAQuery | a query built from a case name and a value | authored in the test | exact | both withheld unless allowed for that search |
| assistant/AC-021 | unit plus integration | tests/test_egress.py::TestTheAuditIsReadableAndExportable and tests/test_handlers.py::TestNothingHasLeftAndItSaysSo | several requests of different kinds, and the audit read through the API | authored in the test | exact | each appears in the audit with host and time as the pair; the audit exports; an empty audit is an empty record |
| assistant/AC-022 | unit | tests/test_egress.py::TestAnUnlistedHostIsRefusedByName | a host outside the allow-list | authored in the test | exact | refused with the host named; no substitute source used |
| assistant/AC-023 | integration (planned) | - | results retained from a search | recorded responses | exact | each stored with address and retrieval date; offered by identifier only |
| assistant/AC-024 | integration (planned) | - | a document contradicting the dataset | authored here | exact | the data value is carried and the disagreement stated |
| assistant/AC-025 | integration (planned) | - | a search returning nothing retained | recorded responses | exact | states nothing was found; no answer from model memory |
| view/AC-024 | integration (planned) | - | four cases in a split of four | authored here | exact | each pane renders its own case |
| view/AC-025 | integration (planned) | - | a split with synchronisation on | authored here | exact | one camera change moves all panes; the export records the state |
| view/AC-026 | integration (planned) | - | two panes with different ranges | authored here | exact | marked not directly comparable; common range offered |
| view/AC-057 | unit | tests/test_ui_mockup_catalog.py | the mockup View component, wrapper CSS and Three.js scene | neutral geometry authored for the mockup | exact | every populated pane mounts the shared Canvas with orbit, representation controls, grid and gizmo; the compact gizmo is top-right with a visible gap below the icon row, unclipped enlarged axis-head circles and enlarged X/Y/Z labels, and the View wrapper has no padding, light frame, invented result label or separate status footer |
| view/AC-027 | integration | tests/test_handlers.py::test_a_pixel_on_the_model_answers_with_a_value_from_the_dataset and tests/test_known_solution.py | a point on a known field value | generated by the test | exact | value, unit, digits and provenance all correct |
| view/AC-028 | integration | tests/test_handlers.py::test_a_cell_field_is_picked_as_a_cell_s_value | a pick on cell data through view.pick | generated by the test | exact | the cell value is reported and named as cell data |
| view/AC-029 | unit | tests/test_pick.py::test_a_point_off_the_model_is_missing_not_the_nearest_value | a probe outside the mesh, and on a missing entry | authored in the test | exact | reported missing, never zero |
| view/AC-094 | unit plus integration | tests/test_missing_values.py and src/ui/state/engine.connected.test.ts against a real engine | the holed grid: three missing points, one missing component, two missing cells; the same grid missing everywhere | generated by tests/demo_case.py | exact | every statistic over the present entries with the caveat and the count; the all-missing field a stated absence with the count; a probe at a missing point an absence with its reason; the pick never a neighbour (XC-303) |
| view/AC-030 | integration (planned) | - | a kept probe | authored here | exact | a variable carrying case, position and time |
| view/AC-031 | integration (planned) | - | a case with ten time steps | generated by the test | exact | current time behaves as a variable and a loop iterates it |
| view/AC-032 | unit plus integration | tests/test_handlers.py::TestTheResultPosition and src/ui/state/engine.connected.test.ts against a real engine | the transient CGNS fixture, two declared steps | generated by the test | exact | every answer - statistics, probe, pick, picture, the report's rows - states which step it is of; the interface's footer and probe readout carry the engine's sentence |
| view/AC-033 | unit plus integration | tests/test_survey.py::TestAPositionOnTheAxis, tests/test_cgns.py::TestReadingAnotherStep and tests/test_handlers.py::TestTheResultPosition | a request for a step past the declared sequence, and for any step but the first of a steady case | authored in the test | exact | refused by name with what there is; nothing is read and nothing nearer is answered - the toolkit's own substitution is measured (E-211) and never reached |
| view/AC-034 | integration plus mockup unit | tests/test_ui_mockup_catalog.py | a nested multi-block file and the Outliner header markup and CSS | generated with the toolkit writers; header and visual density authored in the mockup | exact | View right Outliner preserves source names, types and parent-child relations; root is the first row, no static Dataset button appears, and the mockup uses 10 px names in 27 px rows with layered light-neutral surfaces, a pale-blue plus left-accent selection state and no persistent shortcut footer |
| view/AC-035 | unit plus integration | tests/test_handlers.py::TestStatistics::test_a_region_is_one_part_and_its_numbers_are_that_part_s_with_the_scope_stated and src/ui/state/engine.connected.test.ts against a real engine | an Exodus file the toolkit reads as two parts; the demo cube from the interface | generated in the test | exact | the part's maximum differs from the model's; each answer's scope names the part or the whole case with its part count; a name that is no part is refused with the parts there are |
| view/AC-055 | unit plus integration | tests/test_handlers.py::TestPartVisibility, src/ui/logic/parts.test.ts and src/ui/state/engine.connected.test.ts against a real engine | an Exodus file the toolkit reads as two parts under one block; the demo cube from the interface | generated in the test | exact | a hidden part is not drawn, not picked and not exported; hiding every part or naming a part not there is refused by name; a pick selects the part it read; Ctrl isolates the branch and a block's control reaches its descendants |
| view/AC-056 | unit plus integration | src/ui/logic/parts.test.ts, tests/test_cgns.py and src/ui/state/engine.connected.test.ts against a real engine | a flat file, a nested one with an absent part, nothing loaded | generated in the test | exact | a flat file reads flat, a block per step of the file's path, the absent part where the file put it with the reader's reason and no control, no rows before a load; the unnamed marker is the reader's `block N` |
| view/AC-058 | unit (planned) | - | View objects backed by source and derived definitions | authored in the test | exact | each has View-local identity and remains separate from Dataset and Asset identity |
| view/AC-059 | integration (planned) | - | an Object asset instantiated and then revised or deleted | authored here | exact | the View object retains source id/revision provenance and does not change or disappear silently |
| view/AC-060 | unit | tests/test_ui_mockup_catalog.py | both executable UI catalogues' View property rail and material library | authored in the mockups | exact | current state uses Object and selected-object labels, the reusable category is Object, and Asset remains the library/shelf lifecycle concept |
| view/AC-061 | round-trip integration (planned) | - | a reference mesh with two named indexed UV sets | generated and authored fixture | exact | names, values, indices and interpolation unchanged; no generated set overwrites them |
| view/AC-062 | unit plus integration (planned) | - | an analysis mesh with uniform PBR and field colouring | generated fixture | exact | no UV allocation; reported values identical |
| view/AC-063 | integration (planned) | - | triplanar and each declared projection profile | generated fixture | exact | declared mode, `scale_m` and rotation used; Dataset unchanged |
| view/AC-064 | deterministic integration (planned) | - | one texture-requiring analysis surface generated twice | generated fixture | exact | identical atlas for the same generator, version, parameters and topology; cache invalidates on topology change; canonical data unchanged |
| view/AC-065 | unit plus mockup (planned) | - | authored, pending, generated, degraded and unsupported states | authored UI states | exact | high-level mode, scale, rotation and status visible; no UV editor |
| view/AC-066 | integration (planned) | - | failed atlas with and without a declared triplanar fallback | authored fixture | exact | reason named; only the declared fallback used; otherwise texture unapplied |
| view/AC-067 | unit plus mockup | tests/test_ui_mockup_catalog.py | all eight first-release View-object types plus each Mesh representation | authored UI states and CT-004 schema | exact | each deep-linked state names the selected type and exposes only its type-specific property groups; Mesh has one representation selector, one display-opacity multiplier and no duplicate Edge checkbox; edge modes reveal one object-level colour/width/opacity group; CT-004 3.1 stores one presentation entry per object separately from Material Bindings |
| view/AC-068 | unit plus interaction | tests/test_ui_mockup_catalog.py | viewport and Outliner selection changes including Shift reselect | authored mock scene | exact | whole-View Overall/Rendering/Background/Output precede one aria-hidden non-focusable separator and active-object Object/conditional Text/Materials; scoped accessible names are present; keyboard order crosses the separator; object tabs follow the most recently selected active object while other selected objects remain selected and no aggregate form appears |
| view/AC-069 | unit plus mockup | tests/test_ui_mockup_catalog.py | each View object type, including unsupported states and text/dimension annotation subtypes | authored UI states | exact | unsupported control omitted or reason shown with no plausible enabled default; Text tab exists only for Text/Annotation; Object owns kind/anchor/provenance; Text owns content/typography; other types omit the tab with no empty-font state; derived dimension/label numbers remain non-editable |
| view/AC-070 | schema plus unit | tests/test_ui_mockup_catalog.py | CT-004 material bindings with whole-object, part, disjoint and overlapping element-set targets | authored schema and UI | exact | immutable revision required; several slots accepted; each element resolves one root; overlap refused |
| view/AC-071 | unit plus mockup | tests/test_ui_mockup_catalog.py | data-independent, result-dependent, composite and unresolved Material Assets | authored UI states | exact | one slot/card model; badges derived from requirements; no stored/user-selected material kind |
| view/AC-072 | unit plus mockup | tests/test_ui_mockup_catalog.py | MaterialX graph with two required result inputs and one graph without them | authored UI states | exact | only declared inputs shown with type/association/unit and current binding; no empty analysis form or type picker |
| view/AC-073 | unit plus interaction | tests/test_ui_mockup_catalog.py | selected active object and an incompatible object | authored mock scene | exact | Materials and contextual Text use the shared active object, expose no second target selector or recipient block, and incompatible apply leaves appearance unchanged |
| view/AC-074 | unit plus mockup | tests/test_ui_mockup_catalog.py | Object properties plus data-independent library fixture, labelled data-dependent fixture, resolved live preview and failed live preview | authored primitives and fixture | exact | Object has no material viewer; Materials owns the only right-sidebar live viewer with slots above and editing tabs below; sphere default; cube/plane/cylinder and fixed non-rotating 2D surface accessible at right; combined/PBR/result outputs switch independently at left without a generic Height output; Material property, library and combined output share a shaded sphere icon while Base Color retains a palette; sample labelled; unresolved live target diagnostic magenta |
| view/AC-075 | integration (planned) | - | cancellation, incompatible terminal and accepted unresolved required input | authored fixtures | exact | first two leave bindings unchanged; accepted unresolved instance is repairable, failed and magenta with exact missing input |
| view/AC-076 | unit plus mockup | tests/test_ui_mockup_catalog.py | Material Assets in library and assigned slots, including sample-data and missing-thumbnail states | generated transparent PNG fixtures | exact | same revision reuses the image; name/source/revision/sample/state remain text; no borrowed or generic material image |
| view/AC-077 | unit plus mockup | tests/test_ui_mockup_catalog.py | coordinate-free, textured and result-dependent MaterialX graphs in Basic, Node and Source authoring views | switch tabs, switch Base Color among solid/image/colour-map/formula, edit OpenPBR opacity, edit range endpoints and colour/opacity points, edit valid and invalid source, save a draft and switch slots | exact | three named views stay on one graph and add no duplicate output canvas; Base Color uses four presentation modes with no RGB, Analysis Result or Node Connection mode; typed analysis/coordinate variables appear inside compatible modes; Basic exposes OpenPBR float geometry_opacity separately from Object display opacity; the sidebar colour map contains only variable plus editable endpoint values and a horizontal bar; its dialog places opacity above colour, supports point add/select/move/remove plus exact values, and makes out-of-range values transparent; Basic has no one-option shader picker or generic Height, shows Mapping only for a declared coordinate source and states canonical-metre scale; Node mirrors and retains the active graph and expands centrally; Source names the active file and validates automatically; one shared save revalidates and creates a revision; no manual Validate button or material-kind selector |
| view/AC-078 | JSON Schema plus semantic unit (planned) | - | data-independent, result-dependent and composite CT-011 fixtures | authored MaterialX and manifests | exact | all use one contract and one qualified root; no kind enum or duplicated shader values |
| view/AC-079 | rendering integration (planned) | - | point and cell scalar/vector result inputs across VTK, vtk.js and USD | generated fields | exact | binding identity/component/association/position/unit retained; validity attribute present; shader has no storage access |
| view/AC-080 | round-trip unit (planned) | - | matching, missing and conflicting MaterialX/manifest backlinks | authored fixtures | exact | only matching pair links; originals and hashes retained in every unresolved case |
| view/AC-081 | rendering integration (planned) | - | each missing field/component/association/position/unit/resource/hash/node/backend feature | one fault per fixture | exact | whole target diagnostic magenta; ordinary legend suppressed; exact CT-010 code; no stale pixels or fallback |
| view/AC-082 | rendering integration (planned) | - | one valid field with isolated NaN/missing entries | generated field and validity mask | exact | only affected elements diagnostic; valid colours and canonical numbers unchanged |
| view/AC-083 | parser, security and round-trip integration (planned) | - | unknown node/attribute, include tree, imported implementation and older MaterialX | authored hostile and compatible fixtures | exact | raw bytes/dependencies hashed and retained; parsed graph validated; source implementation not executed; upgrade creates revision |
| view/AC-084 | adapter conformance (planned) | - | every CT-011 required node/channel/terminal on pinned native VTK and vtk.js | generated capability corpus | exact | each feature classified exact/baked/unsupported; bake requires approval and complete provenance |
| view/AC-085 | USD integration (planned) | - | multi-subset graph with exact, baked and unsupported MaterialX features | generated USD package | exact | mtlx sourceAsset/subIdentifier, identity metadata, non-overlap, preview fallback limits and loss report all verified |
| view/AC-086 | filesystem and network isolation integration (planned) | - | traversal, symlink escape, absolute/remote URI, excessive include/dependency/image fixtures | authored hostile package | exact | every escape/refusal named; no network attempted; imported code remains inert |
| view/AC-087 | traceability integration (planned) | - | direct LUT, validated compound transfer and arbitrary untraceable colour mix | generated graphs | exact | first two produce exact legends; arbitrary mix retained but presentable analysis export refused |
| view/AC-088 | invariant unit (planned) | - | rendering Asset named steel plus contradictory engineering properties | authored fixture | exact | applying/editing Material changes no engineering property or reported number |
| view/AC-090 | integration | tests/test_handlers.py::TestRenderingAView::test_a_camera_given_to_the_render_is_used_and_the_definition_is_not_touched and src/ui/state/engine.connected.test.ts against a real engine | a view rendered and picked from a given camera; an orbit and a kept look through the store | generated in the test | exact | the picture changes and the definition, the undo history and the unsaved work do not; keeping the look is one write |
| view/AC-092 | unit plus integration | src/ui/logic/copy.test.ts and src/ui/state/engine.connected.test.ts against a real engine | the statistics and the probe of the demo cube, and a hand-built information view with an absent part | generated in the test | exact | each row carries the value at its digits, the unit or the marker, the digits, the provenance label, the location and the caveats; a missing value is the stated absence; the missing count is an integer; the text is tab-separated with a header |
| view/AC-091 | integration | src/ui/state/engine.connected.test.ts against a real engine, and src/ui/logic/showing.test.ts for the area's footer | a CGNS case with one zone the reader cannot read, loaded through the store | generated by the test | exact | the store holds partial with the absent part's name and the pane's mark is derived from it; the warning names the incompleteness |
| view/AC-089 | unit plus interaction | tests/test_ui_mockup_catalog.py::test_every_right_sidebar_mode_uses_responsibility_specific_editors | View Rendering and Background tabs with Studio, Background Environment and Unlit sources plus visible/hidden environment backgrounds | authored in the mockup | exact | no Lighting rail tab; Rendering owns one conditional Lighting group and strength; Background owns the one environment Asset/rotation, display strength and camera visibility; Background Environment references rather than duplicates them; unlit hides irrelevant controls |
| workspace/AC-026 | integration (planned) | - | a sequence of edits | authored here | exact | each appears in the journal; the saved file is unchanged |
| workspace/AC-027 | integration (planned) | - | a killed process with journalled work | authored here | exact | the work is offered; the saved file is untouched until accepted |
| workspace/AC-028 | unit plus integration | tests/test_workspace_recovery.py::TestASecondOpenIsReadOnlyAndSaysWhoHasIt and tests/test_handlers.py::TestASecondOpenIsReadOnlyThroughTheAPI, and src/ui/state/engine.connected.test.ts against a real engine (a stale lock after a crash: read-only said, saving refused, then taken over on the person's word) | the same workspace opened twice, in process and through the API against a lock another host holds | generated in the test | exact | the second opens read-only and names the holder; the window works in memory; saving and pruning are refused naming the holder |
| workspace/AC-029 | unit plus integration | tests/test_workspace_recovery.py::TestAStaleOrUnreadableLock and tests/test_handlers.py::TestASecondOpenIsReadOnlyThroughTheAPI | a stale lock, an unreadable one, and a take-over asked for | generated in the test | exact | what was found is stated and read-only is offered; a take-over replaces a stale or unreadable lock and never a live holder's |
| workspace/AC-030 | unit (planned) | - | a new view definition | authored in the test | exact | stored in workspaceItems, not templates and not any case |
| workspace/AC-031 | integration (planned) | - | a workspace View edited while case B is selected | authored here | exact | the View changes for its uses; its source template and sibling items do not |
| workspace/AC-032 | integration (planned) | - | save-as-template from View, Graph and Report | authored here | exact | a new template revision in the chosen scope, source item unchanged |
| workspace/AC-033 | unit (planned) | - | a value displayed under a comma-decimal locale | authored in the test | exact | displayed with the locale separator |
| workspace/AC-034 | unit (planned) | - | the same value exported under both locales | authored in the test | exact | byte-identical output with a period separator |
| workspace/AC-035 | integration (planned) | - | a CSV export | authored here | exact | the file states its numeric convention |
| assistant/AC-026 | integration (planned) | - | a provider reporting three models | recorded responses | exact | the three are offered; no compiled-in list appears |
| assistant/AC-027 | integration (planned) | - | a deep-research request | recorded responses | exact | separate permission required; request count and cost stated first |
| assistant/AC-028 | integration (planned) | - | an unavailable model | recorded responses | exact | named, and the previous choice retained |
| assistant/AC-029 | integration (planned) | - | a report overriding the model | authored here | exact | the report uses it; the product setting is unchanged |
| assistant/AC-030 | integration (planned) | - | chat with search not permitted | authored here | exact | refused in the conversation; no request made |
| assistant/AC-031 | integration (planned) | - | chat with search permitted | authored here | exact | query shown, allow-list applied, request audited |
| graph/AC-022 | unit (planned) | - | a mesh with deliberately uneven element sizes | generated in the test | exact | weighted and unweighted differ; each output carries its weighting |
| graph/AC-023 | integration (planned) | - | an unweighted reduction in a table and a report | authored here | exact | labelled unweighted in both |
| graph/AC-024 | unit (planned) | - | a scope with no valid entries | authored in the test | exact | unavailable, not zero |
| report/AC-036 | integration (planned) | - | two runs of one report | authored here | exact | two timestamped folders; the first is intact |
| report/AC-037 | unit (planned) | - | a colliding name pattern | authored in the test | exact | refused before the run, pattern named |
| report/AC-038 | integration (planned) | - | a view using a rainbow map | authored here | exact | the report carries the note |
| pipeline/AC-040 | integration (planned) | - | a run in progress | authored here | exact | viewing works, edits are blocked with a reason |
| pipeline/AC-041 | integration (planned) | - | a finished run with artefacts | authored here | exact | the artefacts are named and can be deleted from the record |
| workspace/AC-036 | integration (planned) | - | a template promoted to shared, and a second workspace | authored here | exact | available in both; the origin workspace still opens alone |
| workspace/AC-037 | integration (planned) | - | a template bound to a workspace variable | authored here | exact | the requirement is written down and reported; promotion proceeds |
| workspace/AC-038 | integration (planned) | - | a template using an embeddable and a non-embeddable asset | authored here | exact | one embedded, one listed by name |
| workspace/AC-039 | integration (planned) | - | the exported file imported into a different workspace | as above | exact | placed, origin recorded, unresolved parts listed |
| workspace/AC-040 | unit (planned) | - | a one-case template applied to a set | authored in the test | exact | refused, arity named |
| workspace/AC-061 | integration (planned) | - | a template with a resolvable and unresolved reference | authored here | exact | preview precedes acceptance; new item records source id/revision and is independent |
| workspace/AC-062 | integration plus mockup unit | tests/test_ui_mockup_catalog.py | multiple View, Graph and Report items | authored here | exact | item lists open, create, duplicate, rename and delete without labelling items Templates |
| workspace/AC-063 | integration (planned) | - | an item created from template revision 1, then template revision 2 | authored here | exact | the existing item is unchanged and retains revision 1 as source provenance |
| pipeline/AC-042 | unit (planned) | - | one item-backed and one template-backed artefact unit | authored in the test | exact | source, id and revision are explicit and neither reference advances silently |
| workspace/AC-064 | mockup unit | tests/test_ui_mockup_catalog.py::test_simulation_view_graph_report_headers_offer_only_named_new_item_creation | Simulation, View, Graph and Report work-area headers | authored in the mockup | exact | each identifies the current item and offers only its matching `＋ 新規シミュレーション`, `＋ 新規ビュー`, `＋ 新規グラフ` or `＋ 新規レポート`; generic New, persistent Template and Save-as-template buttons are absent, and Simulation still displays its r1-unavailable state |
| workspace/AC-065 | mockup unit | tests/test_ui_mockup_catalog.py | collapsed, one-row, expanded, searching, selected and narrow material-library states | authored in the mockup | exact | the shelf sits between centre canvas and instruction bar, keeps its directional toggle chevron visibly rendered in a dedicated title-adjacent slot in both states, preserves the right property height, overlays at narrow width, exposes click/drag/Apply paths, omits a redundant one-row/multi-row icon, is absent in Chat, and the right rail edits current state with `全体` replacing Template |
| workspace/AC-066 | mockup unit | tests/test_ui_mockup_catalog.py | left/right sidebars and open/collapsed material-library states | authored in the mockup | exact | complete inner sidebar boundaries resize width, the complete shelf top boundary resizes height from its rendered pointer-down height without transition, its maximum follows remaining centre-column height, the app never gains vertical scrolling, separator semantics plus pointer and arrow-key paths are present, centre-preserving bounds apply, corner decoration is absent, and hidden/collapsed splitters are absent |
| workspace/AC-041 | integration (planned) | - | an import of six sibling result files | generated by the test | exact | tags proposed from readable signals; none applied until accepted |
| workspace/AC-042 | integration (planned) | - | as above, with a model configured | recorded responses | exact | name-inferred tags appear, marked inferred |
| workspace/AC-043 | integration (planned) | - | a rejected proposal | authored here | exact | not offered again in the session |
| workspace/AC-044 | unit (planned) | - | a variable switched to independent | authored in the test | exact | current value kept; the detachment is stated |
| view/AC-036 | integration (planned) | - | every background kind that ships (OPEN-016) | sample assets | exact | reported values identical with and without each |
| view/AC-037 | integration (planned) | - | a splat scene and a prop model | sample assets | exact | the cost against LIM-009 is stated before applying |
| view/AC-038 | unit (planned) | - | background content over LIM-009 | authored in the test | exact | refused; result geometry untouched |
| view/AC-039 | integration (planned) | - | one view definition in 2D and 3D | as ingest | exact | the three-view arrangement uses the same definition |
| view/AC-040 | unit (planned) | - | a dimension line on a known distance | generated by the test | exact | value from canonical data with unit and digits |
| view/AC-041 | integration (planned) | - | a video from a named camera path | sample camera path | exact | path and playback speed are stated |
| view/AC-042 | integration (planned) | - | a transient case rendered to video | as ingest | exact | the time mapping is stated and reaches the report |
| graph/AC-025 | integration (planned) | - | a field over two independent variables | generated by the test | exact | surface, scatter and contour kinds all available |
| graph/AC-026 | integration (planned) | - | an exported 3D graph | as above | exact | projection and view direction stated on the output |
| graph/AC-027 | unit | tests/test_ui_mockup_catalog.py::test_graph_canvas_has_no_redundant_apply_button | the default Graph display heading and shared material-library footer | authored in the mockup | exact | the Graph heading contains no primary Apply button while the separately owned material-library Apply path remains present |
| report/AC-039 | integration (planned) | - | two workspaces with comparable cases | authored here | exact | one document; every value names its workspace |
| report/AC-040 | integration (planned) | - | as above with one workspace moved away | as above | exact | report produced; the missing source named |
| report/AC-041 | integration (planned) | - | one definition exported to every kind | authored here | exact | all nine kinds produced from the one definition |
| report/AC-042 | integration (planned) | - | a 3D block exported to plain text | authored here | exact | the substitution is stated, not silent |
| diff/AC-001 | unit (planned) | - | two cases on one generated mesh | generated in the test | exact | difference matches the hand-computed value |
| diff/AC-002 | unit (planned) | - | two cases with global identifiers in different array order | generated in the test | exact | matched by identifier, not position |
| diff/AC-003 | unit (planned) | - | fields declared in different units | authored in the test | exact | refused, both units named |
| diff/AC-004 | unit (planned) | - | a location missing in one case | authored in the test | exact | missing in the diff, never zero |
| diff/AC-005 | integration (planned) | - | two cases on different meshes | generated in the test | exact | the run refuses to proceed until the target is named |
| diff/AC-006 | integration (planned) | - | as above | as above | exact | direction, outside-point count and round-trip error all travel with the result |
| diff/AC-007 | unit (planned) | - | a target point outside the source mesh | generated in the test | exact | missing, never extrapolated |
| diff/AC-008 | integration (planned) | - | a cross-mesh diff in a report | as above | exact | the report states both contributions are present |
| diff/AC-009 | unit (planned) | - | any computed diff | generated in the test | exact | a field with unit and provenance naming both cases and the method |
| diff/AC-010 | unit (planned) | - | a relative diff with a zero reference | authored in the test | exact | reference required; zero reports undefined, not infinite |
| operations/AC-001 | manual | - | a first launch with no configuration | the installer under test | - | the View area opens with a rendered result |
| operations/AC-002 | integration (planned) | - | the shipped sample workspace | generated by this project | exact | no third-party result file is present in the installation |
| operations/AC-003 | manual | - | first launch, empty chosen | the installer under test | - | the View area opens with its empty state and a drop target |
| operations/AC-004 | manual | - | the tutorial from a first launch | as above | - | each step points at a live control and advances on use |
| operations/AC-005 | integration (planned) | - | a dismissed tutorial | authored here | exact | the position is recorded and resuming continues from it |
| operations/AC-006 | manual | - | an unexpected action during the tutorial | as above | - | the action happens and the tutorial re-points |
| operations/AC-007 | unit (planned) | - | a session that loads data and renders | generated in the test | exact | no field value appears in any log line |
| operations/AC-008 | unit plus integration | tests/test_diagnostics.py::TestTheManifestExistsBeforeTheBundle, tests/test_handlers.py::TestASupportBundle and src/ui/state/engine.connected.test.ts against a real engine | a session with an open document, a loaded file and a refused command; the list asked with and without the person's information | authored in the tests and the generated sample (XC-298) | exact | the list names the log, the product, the environment and the workspace always, and each case name and path individually only when chosen; a bundle without the list shown is refused (XC-302) |
| operations/AC-031 | unit plus integration | tests/test_diagnostics.py::TestTheBundleOnDisk, tests/test_handlers.py::TestASupportBundle and src/ui/state/engine.connected.test.ts against a real engine | a log holding a refusal whose reason names a path; bundles with names only and with both; a path already taken | authored in the tests | exact | the archive holds exactly what was listed; the reason is replaced unless both were included and kept when they were; a second write to the same path is refused and no `.writing` file remains (XC-302) |
| operations/AC-032 | unit plus measurement | src/ui/logic/startup.test.ts, src/ui/state/reachability.test.ts; spike/measure_launch.py for the timeline | the shell's status as starting, absent and exited; three launches of the development shell | authored in the tests; measured here | exact | the words count the seconds, name a slow start without a promised time, and give a failed start's reason; the store holds starting apart from absent and exited; the timeline is in spike/results.json launch (XC-304) |
| operations/AC-033 | unit plus measurement | src/ui/logic/budgets.test.ts; src/ui/state/engine.connected.test.ts for the switch timing | a switch within and over LIM-011; a launch within and over LIM-010; six switches with both cases loaded | authored in the tests; measured here | exact | nothing said within the budget; the time and the budget named over it; the store records every switch's time (XC-305) |
| operations/AC-009 | unit | tests/test_egress.py::TestASupportBundleLeavesOnlyWithConsent | a bundle transfer | authored in the test | exact | consent required; the transfer is in the outbound audit |
| operations/AC-010 | unit | tests/test_check_commands.py::test_an_operation_missing_from_the_schema_fails | the CT-003 catalogue and its schema | the specification itself | exact | an operation in one and not the other fails the gate |
| operations/AC-011 | unit (planned) | - | an interface action mutating state directly | authored in the test | exact | the gate fails |
| operations/AC-012 | unit | tests/test_check_commands.py::test_the_gate_always_states_what_it_did_not_check | a run with no interface code present | the repository as it stands | exact | the output names what could not be checked |
| operations/AC-013 | integration (planned) | - | the command catalogue | the specification itself | exact | every command has a keyboard route |
| operations/AC-030 | unit plus integration | src/ui/logic/palette.test.ts and src/ui/state/engine.connected.test.ts against a real engine | the loaded demo case as the palette's context | generated in the test | exact | the runnable set is exactly the answered operations whose required parameters the context holds; the destructive one, an unimplemented one and one needing another parameter each carry their reason; a run answers as the engine did and a refusal comes back as a reason with the window's refusal untouched |
| operations/AC-029 | unit plus integration | src/ui/logic/commands.test.ts, tests/test_handlers.py::TestWhatThisBuildRegisters::test_operations_list_what_this_build_answers_and_what_it_does_not and src/ui/state/engine.connected.test.ts against a real engine | the generated catalogue and a real engine's registry | the specification itself | exact | every operation is a row with class, parameters and answer members; the engine's registered and unimplemented halves are the whole catalogue and disjoint; a row says answered, unimplemented or not yet known; no row shows a key |
| operations/AC-014 | integration (planned) | - | an unknown caller | authored here | exact | refused by default |
| operations/AC-015 | integration (planned) | - | a caller authorised for one workspace | authored here | exact | the other workspace is refused |
| operations/AC-016 | integration (planned) | - | a sequence of authenticated operations | authored here | exact | each carries the issuing identity in the audit |
| operations/AC-017 | integration (planned) | - | an agent caller | authored here | exact | rights equal its authenticated identity, no more |
| operations/AC-018 | integration (planned) | - | an update carrying a changed sample | authored here | exact | the new version is added; user copies are byte-identical afterwards |
| operations/AC-019 | integration (planned) | - | a copy recording a sample origin | as above | exact | a newer-version notice appears and adopting is a user action |
| ingest/AC-035 | integration (planned) | - | a file written with global identifiers | generated with the toolkit writers | exact | identifiers preserved and used for extreme-value reporting |
| ingest/AC-036 | integration (planned) | - | a file with no identifiers | generated in the test | exact | absence stated; no index presented as an identifier |
| ingest/AC-037 | integration (planned) | - | a measured data file against a case | authored here | exact | held as measurement data and usable as a source of numbers |
| ingest/AC-038 | unit (planned) | - | measured values with uncertainties | authored in the test | exact | uncertainty stays with the value through a comparison |
| ingest/AC-039 | unit (planned) | - | measured values with no unit | authored in the test | exact | marked undeclared, not matched to the computed field |
| ingest/AC-040 | integration (planned) | - | a file with integration-point values | generated with the toolkit writers | exact | read as written; no extrapolation to nodes |
| view/AC-043 | unit plus integration | tests/test_derived.py, tests/test_handlers.py::TestDerivedQuantities and src/ui/state/engine.connected.test.ts against a real engine | a vector and a symmetric tensor with answers known by hand, in memory and as fields.vtu | generated in the test | exact | each built entry's value and recorded formula are correct; a field of several components is refused as one number; the entries not built are refused by name |
| view/AC-044 | unit plus integration | tests/test_derived.py::TestTheTensorEntries::test_principal_values_are_three_fields_ordered_largest_to_smallest_and_say_so and tests/test_handlers.py::TestDerivedQuantities | a tensor with distinct eigenvalues | generated in the test | exact | three fields ordered largest to smallest, the ordering in the conventions |
| view/AC-045 | unit | tests/test_derived.py::TestTheVectorEntries::test_a_vector_component_names_its_frame | a vector component | generated in the test | exact | the frame is named, global Cartesian by default |
| view/AC-046 | unit plus integration | tests/test_derived.py::TestWhatIsRefused::test_a_frame_that_does_not_exist_is_refused_by_name and tests/test_handlers.py::TestDerivedQuantities::test_what_is_not_derived_is_refused_by_name | a cylindrical component with no frame defined | authored in the test | exact | refused, naming the frame asked for and the one that exists |
| view/AC-093 | unit plus integration | tests/test_handlers.py::TestStatistics::test_a_cell_field_answers_both_numbers_and_the_spread_at_the_peak, tests/test_handlers.py::TestStatistics::test_the_document_states_both_numbers_for_a_cell_field, src/ui/logic/copy.test.ts and src/ui/state/engine.connected.test.ts against a real engine | E-144's bar as a file: five hexahedra carrying 10, 20, 200, 20, 10 | generated in the test | exact | the element maximum is 200 labelled unaveraged, the averaged maximum 110 carries the averaged caveat and the spread 180 at its node; the document states both with the spread as a row; a point field carries no averaging members |
| view/AC-047 | unit (planned) | - | a two-material mesh | generated in the test | exact | no averaging across the interface; the result is labelled averaged |
| ingest/AC-041 | integration (planned) | - | a modal result file | generated with the toolkit writers | exact | indexed by mode number with eigenfrequencies carried |
| ingest/AC-042 | integration (planned) | - | a harmonic result file | generated with the toolkit writers | exact | real and imaginary kept together, indexed by frequency |
| ingest/AC-043 | unit (planned) | - | a file with an ambiguous index | authored in the test | exact | reported unknown; not presented as time |
| ingest/AC-044 | integration (planned) | - | a graph mixing modal and transient results | authored here | exact | the difference of axes is stated |
| view/AC-048 | integration (planned) | - | a case with displacement | generated by the test | exact | drawn at 1.0 by default; auto-scale available in one action |
| view/AC-049 | integration (planned) | - | the same case at scale 50 | as above | exact | the factor appears in the view and in the exported image |
| view/AC-050 | unit (planned) | - | a probe at scale 1.0 and at scale 50 | generated in the test | exact | identical undeformed position and value in both |
| view/AC-051 | integration (planned) | - | a stored streamline definition run twice | generated by the test | exact | identical geometry; a changed step size changes it |
| view/AC-052 | integration (planned) | - | streamlines beside measured quantities | authored here | exact | labelled a derived visualisation |
| view/AC-053 | unit (planned) | - | a length shown in millimetres | authored in the test | exact | display changes; stored bytes and computed results do not |
| view/AC-054 | unit (planned) | - | an undeclared quantity | authored in the test | exact | display unit refused; undeclared marker kept |
| workspace/AC-045 | unit (planned) | - | each transition of XC-136 | authored in the test | exact | only the listed transitions are permitted; the tree shows the state |
| workspace/AC-046 | integration (planned) | - | a case whose input file is removed | authored here | exact | moves to unresolved; definitions retained |
| workspace/AC-047 | integration (planned) | - | a pipeline over cases in several states | authored here | exact | skipping follows the case state |
| report/AC-043 | integration (planned) | - | an exported report | authored here | exact | input content identity and workspace version recorded |
| report/AC-044 | integration (planned) | - | as above, with an input changed afterwards | as above | exact | the deliverable is reported as produced from changed data |
| assistant/AC-032 | integration (planned) | - | the recorded evaluation set | authored with the set | stated bar | pass rate reported against the bar |
| assistant/AC-033 | integration (planned) | - | a changed model or prompt | recorded responses | stated bar | the set re-runs and a regression is visible |
| assistant/AC-034 | integration (planned) | - | a build with no model configured | authored here | exact | reported skipped, never reported as a pass |
| operations/AC-020 | measurement, and manual on the installer | spike/measure_launch.py (record: spike/results.json launch, E-222) | three launches of the development shell and of the packaged application on a warm cache; the installer under test | this machine; the installer under test | LIM-010 | a drawn sample within the budget as a sum of measured segments - 0.63 s packaged and warm here, 1.05 s on the first launch after packaging; a cold start is not measured here (XC-305) |
| operations/AC-021 | integration plus measurement | src/ui/logic/subject.test.ts and src/ui/state/engine.connected.test.ts against a real engine | selecting a case with every area open, one area pinned | the demo workspace with a second case added | exact; LIM-011 once numbered | every following area shows the selected case and says so, the pinned one stays, and the budget is measured by hand until LIM-011 has a value; six switches with both cases loaded settle in 99-110 ms here, recorded in spike/results.json selection (E-223), and the store says when one is over LIM-011 (XC-305) |
| operations/AC-022 | unit (planned) | - | the shared-component table and the interface source | the specification and the repository | exact | each named component resolves to one implementation in its owning module |
| operations/AC-023 | unit (planned) | - | a deliberate second implementation | authored in the test | exact | the gate fails and names both places |
| operations/AC-024 | unit | tests/test_check_commands.py::test_the_gate_reports_shared_components_as_unexamined | a run with no interface code | the repository as it stands | exact | reported as not examined, never as unique |
| operations/AC-025 | unit (planned) | - | one instance of each reason code | authored in the test | exact | every refusal and failure validates against the CT-010 schema |
| operations/AC-026 | unit (planned) | - | a failure carrying a path and a field value | authored in the test | exact | neither appears in the summary or in any log line |
| operations/AC-027 | unit (planned) | - | a permission refusal and a read failure | authored in the test | exact | reason codes fall in different groups |
| operations/AC-028 | integration (planned) | - | a headless run with a partial result, and one with a failure | authored here | exact | zero for the first, non-zero for the second |
| workspace/AC-048 | integration (planned) | - | a workspace with data and shared assets | authored here | exact | size stated first; contents match what was promised |
| workspace/AC-049 | integration (planned) | - | a workspace with a linked folder and a non-redistributable asset | authored here | exact | both named as not included |
| workspace/AC-050 | integration (planned) | - | a pack created without data | as above | exact | opens with every case unresolved |
| workspace/AC-051 | unit | tests/test_units.py::test_a_temperature_difference_carries_no_offset | authored in the test | exact | absolute and difference convert by different rules |
| workspace/AC-052 | unit plus integration | tests/test_output_and_time.py::TestHowMuchSpaceIsInUse and tests/test_handlers.py::TestOutputIsListedPlannedAndPrunedByName | runs under a workspace's output folder, one with a record and one without; a workspace past the limit | generated by the test | exact | space reported per run and in total with each time and where it came from; pruning offered by run, oldest first, never the newest |
| workspace/AC-053 | unit plus integration | tests/test_output_and_time.py::TestPruningNamesEverythingItWouldRemove and tests/test_handlers.py::TestOutputIsListedPlannedAndPrunedByName, src/ui/logic/output.test.ts for the page's lines and src/ui/state/engine.connected.test.ts against a real engine | a prune of chosen runs through the API, with and without authorisation, after the folder changed, and of a run holding a recorded input | as above | exact | every file named before deletion; the act needs allowDestructive; exactly the plan's files go and the records stay; a changed folder and a run holding an input are refused with nothing deleted |
| workspace/AC-054 | unit plus integration | tests/test_recorded_time_everywhere.py::TestNoContractCarriesATimeAsAString and tests/test_recorded_time_everywhere.py::TestEveryTimeTheEngineWritesIsThePair, and src/ui/logic/time.test.ts for the display | every contract schema; a session whose clock stands at noon in Osaka opening, inspecting, loading, detaching, logging and describing a deliverable; a version-4 document | the schemas themselves; generated in the test | exact | no schema property ends in Iso or Utc and every recorded time is a reference to CT-001 recordedTime; the history, inspection and provenance answers, the source and detachment records and the log line carry {utc, offsetMinutes} with the session's offset; the deliverable names the recording zone; a version-4 document lifts with the offset null and the open says so; the interface shows every time in the reader's zone, naming the recording zone when it differs and saying so when it is unknown |
| operations/AC-002 | unit plus integration | tests/test_sample.py, tests/test_handlers.py::TestTheSample and tests/test_known_solution.py | the generated beam under 100 N and 150 N | generated by the engine | exact | 615 points, 320 cells, the maximum stress `F L (h/2) / I`, the tip deflection `F L^3 / (3 E I)`, no unit in the file, the document's declared units applied on load (E-220) |
| workspace/AC-076 | unit plus integration | tests/test_handlers.py::TestANewWorkspace, src/shell/recent.test.ts, src/ui/logic/home.test.ts and src/ui/state/engine.connected.test.ts against a real engine | a new document at a chosen path, the same path again, a missing folder; the recent list remembered, forgotten, damaged; a query and tags over three entries | authored in the tests | exact | written with one case and opened; refused by name with nothing touched; the list newest first, one entry per path, twenty at most; the narrowing as stated (XC-297) |
| workspace/AC-077 | unit | src/shell/recent.test.ts and src/shell/profile.test.ts | a list of two entries cleared, and cleared when never written; the profile layout under one root | authored in the tests | exact | the list reads empty and the file no longer holds the paths; the recent file is under the profile root and inside neither the engine root nor the log directory (XC-300) |
| workspace/AC-078 | unit plus integration, and measurement | tests/test_workspace_hierarchy.py::TestTheTreeIsBoundedInCases, tests/test_handlers.py::TestAWorkspaceIsBoundedInCases, src/ui/logic/subject.scale.test.ts; spike/measure_workspace_cases.py (E-225) | a tree at the limit and one past it; a document one over the limit; trees of 500 to 20,000 cases | authored in the tests; generated by the script | exact | the add past the limit refused by name; the document opened whole with the warning naming the count and the limit, listed in full, and saved; the tree built whole at every size, in milliseconds (XC-306) |
| workspace/AC-055 | integration (planned) | - | the application shell in workspace and list states | the retained cae-saa-s mockup | exact | labels are Workspace and Workspace list; Case list is absent |
| workspace/AC-056 | integration (planned) | - | three workspaces with no loaded result values | authored here | exact | search, filters, grid/list and create are present; cards contain no invented analysis values |
| workspace/AC-057 | unit | tests/test_ui_mockup_catalog.py | both sidebars and the work toolbar in every area | authored in the mockup | exact | one toggle per side stays at the toolbar edge; neither sidebar duplicates it, the left omits its plus-button title row, and every non-chat right sidebar omits generic mode-collection headings and horizontal tabs in favour of a vertical icon rail with a visible selected Japanese label only, no repeated work-area name, named tooltips and tab semantics; View keeps one rail with an aria-hidden non-focusable scope separator |
| workspace/AC-058 | unit (planned) | - | the shell menu model | the retained cae-saa-s mockup | exact | File, Edit, View, Filter, Tools and Help reach the shared command surface |
| workspace/AC-059 | unit | tests/test_ui_mockup_catalog.py | the retained thumbnail files, Workspace-list card markup and grid CSS | copied from cae-saa-s and checked byte-for-byte | exact | four assigned files are identical, desktop uses four columns, the viewport is 4:3 with cover fill, and the mock-only status is visible |
| workspace/AC-060 | unit | tests/test_ui_mockup_catalog.py | the named View, Graph and Report material-library categories, the Simulation definition and the shared shelf | authored in the mockup | exact | every named category shares horizontal Sample/Original sources, one primary text search, a Tag multi-select trigger with suggestion-list semantics and removable chips, a sort menu limited to Default and Name directions, and result/empty states carrying that category's icon and name; the internal Font kind is labelled Text in every user-facing category tab; mock cards contain no analysis values and Simulation has no shelf |
| workspace/AC-067 | unit | tests/test_ui_mockup_catalog.py::test_graph_and_report_output_tabs_are_last_without_detaching_from_the_tab_sequence | Graph and Report right-sidebar tab definitions and rail CSS | authored in the mockup | exact | each mode has one Output tab after Detail in DOM order, uses the ordinary tab gap and has no physical-bottom docking class or automatic margin |
| workspace/AC-068 | integration (planned) | - | one Workspace with three differently named Simulation flows | authored here | exact | the list creates, opens, duplicates, renames, reorders and deletes concrete Simulation items independently of Cases and Pipelines |
| workspace/AC-069 | unit (planned) | - | an empty draft, an unresolved flow and a valid flow with two external-solver execution conditions | authored here | exact | drafts persist, only the valid non-empty resolved flow is executable, and no invalid flow succeeds as a no-op |
| workspace/AC-070 | round-trip integration (planned) | - | two saved Simulation flows with distinct adapters, input references and parameter bindings | authored here | exact | identity, name, revision, order and every declared condition are identical after save and reopen; no output or value appears without a run |
| workspace/AC-071 | integration (planned) | - | a Pipeline pinned to Simulation revision 1 followed by an edit producing revision 2 | authored here | exact | the unit retains revision 1 until explicitly updated and adds only Cases from successful solver executions |
| workspace/AC-072 | unit | tests/test_ui_mockup_catalog.py | both executable UI catalogues' View current-state and material-library labels | authored in the mockups | exact | current-state controls say Object, the reusable View category says Object, and reusable-resource lifecycle remains Asset |
| workspace/AC-073 | unit plus responsive mockup | tests/test_ui_mockup_catalog.py | six top work-area tabs and View, Graph and Report material-library category groups at desktop and narrow widths | authored in the mockup | exact | one stable width within each group; icon-only top tabs at the narrow breakpoint; equal readable library tabs retain horizontal overflow |
| workspace/AC-074 | unit plus interaction | tests/test_ui_mockup_catalog.py::test_every_right_sidebar_mode_uses_responsibility_specific_editors | every right-rail tab in Simulation, Automation, View, Graph, Report, Settings and Network, including dependent mode choices and unavailable states | authored in the mockup | exact | no generic placeholder editor remains; each tab exposes only responsibility-specific controls; switching background/output/dimension/commentary/material mapping choices reveals only applicable fields; unsupported solvers, renderers, variables, models and permissions remain named; Graph and Report expose their contract output families |
| workspace/AC-075 | unit plus integration | tests/test_workspace_items.py::TestTheWorkspaceIsBoundedInItems and tests/test_handlers.py::TestAWorkspaceIsBoundedInItems | a document holding LIM-016 items, and one holding one more | generated in the test | exact | creation past the limit is refused naming LIM-016 and the count in each list; the document past it opens with every item kept and a warning naming the count and the limit, and saves |
| assistant/AC-046 | unit plus integration | tests/test_handlers.py::TestNothingHasLeftAndItSaysSo, src/ui/logic/egress.test.ts for the pages' lines and src/ui/state/engine.connected.test.ts against a real engine | a session with no transport, before and after a refused request; the engine's answers as the pages show them | generated in the test | exact | capabilities say no way out, nothing permitted and how many entries; the audit lists the refused request with host, exact content and time, and is empty as a record before it; the settings page lists the facts and the network page the audit lines, an empty audit as an empty record |
| assistant/AC-035 | unit (planned) | - | the contracts as they stand | the specification itself | exact | the description matches the catalogue exactly |
| assistant/AC-036 | unit (planned) | - | a workspace with loaded data | generated in the test | exact | no array of field values appears in what is sent |
| assistant/AC-037 | unit (planned) | - | an operation added to the contracts | authored in the test | exact | the generated description changes with no second edit |
| assistant/AC-038 | integration (planned) | - | one instruction submitted from each UI surface | authored here | exact | each appears once in one ordered active-conversation history and both surfaces render the same response and activity |
| assistant/AC-039 | integration (planned) | - | a draft, pending response, confirmation and failure while changing work areas | authored here | exact | conversation id and every named state survive compact/full presentation changes |
| assistant/AC-040 | integration (planned) | - | two conversations selected alternately in Chat | authored here | exact | the instruction bar follows the active selection and no private quick-command conversation exists |
| assistant/AC-041 | parametrized integration (planned) | - | equivalent prompts through instruction bar and Chat | authored here | exact | command mapping, validation, undo, permissions, provenance and audit results are identical |
| assistant/AC-042 | unit | tests/test_ui_mockup_catalog.py::test_work_areas_offer_one_shared_conversation_in_a_right_overlay_drawer | the work-area shell, central surface and existing right sidebar | authored in the mockup | exact | a dismissible right overlay drawer exists inside the central surface and opens the same Chat conversation without replacing the properties sidebar |
| assistant/AC-043 | unit | tests/test_ui_mockup_catalog.py::test_work_areas_offer_one_shared_conversation_in_a_right_overlay_drawer | closed and open work-area assistant presentations | authored in the mockup | exact | the compact input is absent while the drawer renders the one composer at its bottom |
| assistant/AC-044 | integration (planned) | - | one conversation with a draft and pending response across compact, drawer and Chat presentations | authored here | exact | id, ordered history, draft and pending state are unchanged and no message is copied or replayed |
| assistant/AC-045 | responsive integration (planned) | - | the assistant drawer at desktop and narrow centre widths | authored here | exact | it remains dismissible, may cover only the central surface, and causes no application-level overflow |
