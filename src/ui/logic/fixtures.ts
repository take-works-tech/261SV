/* Fixtures the ui-logic tests share. One definition: two tests each declaring `540` are two values
 * waiting to differ, which is what validate/check_constant_duplication.py exists to catch. */

/** Osaka's offset, minutes east of UTC - the zone the Python tests also stand in. */
export const OSAKA = 540;
